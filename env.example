# Variables de entorno opcionales para el backend
# Copia este archivo a .env y ajusta los valores si es necesario
# NOTA: En Render, puedes configurar estas variables en el dashboard

# CORS: Orígenes permitidos (separados por comas)
# Por defecto permite todos los orígenes (*) para desarrollo
# En producción, especifica los orígenes exactos:
# Ejemplo: ALLOWED_ORIGINS=http://localhost:5173,https://tu-dominio.com
# Si no defines esta variable, se permite * (todos los orígenes)

ALLOWED_ORIGINS=*

# Puerto del servidor (Render usa $PORT automáticamente, no necesitas configurarlo aquí)
# PORT=8000

